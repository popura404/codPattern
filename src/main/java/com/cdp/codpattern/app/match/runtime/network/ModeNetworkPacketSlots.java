package com.cdp.codpattern.app.match.runtime.network;

/** Stable legacy slots interleaved inside the shared FPSMatch packet sequence. */
public final class ModeNetworkPacketSlots {
    public static final String FPSM_MODE_TOOL_ACTION = "fpsm.mode_tool_action";
    public static final String FPSM_MODE_TOOL_SCREEN = "fpsm.mode_tool_screen";

    private ModeNetworkPacketSlots() {
    }
}
