/**
 * Forge-specific adapters: commands, events, and networking.
 *
 * Dependency rule:
 * - May depend on Forge APIs and {@code com.cdp.codpattern.app.*}.
 * - Should avoid direct third-party mod API usage; route through {@code compat}.
 */
package com.cdp.codpattern.adapter.forge;
